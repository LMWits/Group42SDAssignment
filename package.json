{
  "dependencies": {
    "@azure/msal-node": "^3.5.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "mocha": "^11.1.0",
    "nyc": "^17.1.0"
  },
  "scripts": {
    "test": "mocha --recursive 'test/**/*.js'",
    "coverage": "nyc --reporter=lcov --reporter=text-summary mocha --recursive 'test/**/*.js'"
  }
}
